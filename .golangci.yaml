run:
  go: 1.18

linters:
  # Enable extra linters besides the default ones
  enable:
    - goimports
    - godot
    - goconst
    - govet
    - gofmt
    - unconvert
    - misspell
    - whitespace
    - revive